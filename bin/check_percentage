#!/usr/bin/env ruby

require_relative '../tweetbot'

puts "Running at #{Time.now}"
session = TrumpPercentageChecker.new

if session.now >= session.start_time && session.now <= session.end_time
  session.check
else
  puts "Out of the time range."
end
